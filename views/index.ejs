<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <title>Gold Beyond</title>
    <style>
        body {
          font-family: Arial, sans-serif;
          margin: 0;
          padding: 0;
          color: #333;
        }
    
        .hero {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 50px;
            background-image: url(images/home-image.png); 
            background-size: cover;
            background-position: center;
            height: 350px;
            color: #4e3a2e;
        }

        .hero-content {
            max-width: 50%;
            text-align: left;
        }

        .hero-content h1 {
            font-family: The Youngest;
            font-size: 3.5vw;
            color: #4e3a2e;
            margin-bottom: 20px;
            margin-left: 35px;
        }

        .hero-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
            max-width: 300px;
            margin-right: 70px;
        }

        .hero-buttons a{
            text-decoration: none;
        }

        .hero-buttons button {
            background-color: #4e3a2e;
            color: #fff;
            padding: 15px 30px;
            border: none;
            cursor: pointer;
            font-size: 1.2vw;
            text-align: center;
            width: 26vw;
        }

        .hero-buttons button:hover {
            background-color: #c4966b;
        }
    
        .collections {
          padding: 2rem 1rem;
          text-align: center;
        }
    
        .section-title {
          font-size: 2rem;
          color: #a86e3e;
          margin-bottom: 2rem;
        }
    
        .categories-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
          gap: 1rem;
          padding: 0 1rem;
        }
    
        .category-card {
          background-color: #f6f3ee;
          padding: 1rem;
          border-radius: 7px;
        }
    
        .category-image {
          width:100%;
          border-radius: 7px;
        }
    
        .category-name {
          margin-top: 1rem;
          font-size: 1.2rem;
          color: #555;
        }

.category-card-link {
    text-decoration: none;
    color: inherit;
    text-align: left;
}

.category-card-link:hover .category-card {
    transform: scale(1.05);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
}

    
        footer {
          background-color: #a86e3e;
          text-align: center;
          color: white;
          padding: 1rem 0;
          margin-top: 2rem;
        }
    
        .footer-text {
          margin: 0;
        }
      </style>
    </head>

    <%- include('partials/navbar.ejs') %>

    <body>
      <!-- Hero Section -->
      <main>
        <section class="hero">
          <div class="hero-content">
              <h1>“Crafted to Go <br>Beyond Where<br> Memory Lasts”</h1>
          </div>
          <div class="hero-buttons">
            <a href="/jewelry"><button>SHOP FOR JEWELRY</button></a> 
            <a href="/appointment"><button>MEET OUR ARTISANS</button></a>
          </div>
        </section>
    
        <!-- Collections Section -->
        <section class="collections">
          <h2 class="section-title">Our Collections</h2>
          <div class="categories-grid">
            <% categories.forEach(category => { %>
              <a href="/<%= category.categoryName.toLowerCase() %>" class="category-card-link">
                <div class="category-card">
                  <img src="/images/<%= category.categoryImg %>" alt="<%= category.categoryName %>" class="category-image">
                  <h3 class="category-name"><%= category.categoryName %></h3>
                </div>
              </a>
            <% }); %>
          </div>
        </section>
      </main>
    
      <!-- Footer Section -->
      <footer>
        <p class="footer-text">&copy; 2024 Gold Beyond. All rights reserved.</p>
      </footer>
    </body>
    </html>
    